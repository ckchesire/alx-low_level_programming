#include "main.h"
/**
 * mul - function to multiply two numbers
 * @a: First integer
 * @b: Second integer
 * Return: return the multiple of the integers
 */
int mul(int a, int b)
{
	return (a * b);
}
